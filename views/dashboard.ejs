<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=<device-width>, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        header {
            text-align: center;
            margin: 0;
        }

        header img {
            max-width: 100%;
            height: auto;
        }

        body {
            background-color: #f8f9fa;
        }

        .container {
            max-width: 800px;
            margin-top: 50px;
        }

        .card {
            border: none;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            transition: box-shadow 0.3s;
            width: 250px;
            margin: 20px;
            background-color: #ffffff;
        }

        .card:hover {
            box-shadow: 0 0 30px rgba(0, 0, 0, 0.2);
        }

        .card-img-top {
            border-top-left-radius: 10px;
            border-top-right-radius: 10px;
            width: 100%;
            height: 200px;
            object-fit: cover;
        }

        .card-body {
            padding: 20px;
        }

        .card-title {
            font-size: 1rem;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .card-text {
            color: #6c757d;
            font-size: 14px;
            margin-bottom: 5px;
        }

        .btn-primary {
            background-color: #FF9900;
            border-color: #FF9900;
            width: 100%;
        }

        .btn-primary:hover {
            background-color: #FFA940;
            border-color: #FFA940;
        }

        .container1 {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
            margin-bottom: 20px;
        }

        form {
            display: flex;
            justify-content: center;
            align-items: center;
        }

        input[type="text"] {
            padding: 10px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 4px;
            flex: 1;
            margin-right: 10px;
        }

        button {
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #0056b3;
        }

        .book-available {
            background-color: #d4edda;
            /* Light green background */
            border: 1px solid #c3e6cb;
            /* Light green border */
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 10px;
        }


        .book-not-available {
            background-color: #da7c6e;
            /* Light green background */
            border: 1px solid #c3e6cb;
            /* Light green border */
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 10px;
        }

        /* Add more styles as needed */
    </style>
</head>

<body>
    <header>
        <a href="https://sjbit.edu.in/" target="_blank"><img src="/images/header.jpg.png" alt="Header Image"></a>
    </header>
    <div class="container1">
        <form id="searchForm" action="/users/search" method="GET">
            <input type="text" id="searchInput" name="query" placeholder="Enter Book Name">
            <button type="submit">Search</button>
        </form>
    </div>

    <div class="container">
        <div class="row">
            <% books.forEach(book=> { %>
                <div class="col-md-4">
                    <div class="card">
                        <img src="<%= book.book_url %>" class="card-img-top" alt="Book Cover">
                        <div class="card-body">
                            <h5 class="card-title">
                                <%= book.title %>
                            </h5>
                            <p class="card-text">Author: <%= book.author_name %>
                            </p>
                            <p class="card-text">Published Year: <%= book.published_year %>
                            </p>
                            <% if (book.availability) { %>
                                <div class="book-available">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" id="book_<%= book.book_id %>"
                                            value="<%= book.book_id %>">
                                        <label class="form-check-label" for="book_<%= book.book_id %>">Book
                                            Available</label>
                                    </div>
                                </div>
                                <% } else { %>
                                    <div class="book-not-available">
                                        <p>Book Not Available</p>
                                    </div>
                                    <% } %>
                        </div>
                    </div>
                </div>
                <% }); %>
        </div>
    </div>


</body>

</html>