<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-image: url('/images/lib_1.jpg');
            background-size: cover;
            background-position: center;
            height: 100vh;
            margin: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        header {
            text-align: center;
            margin: 0;
            padding: 10px;
            background-color: rgba(0, 0, 0, 0.5);
            width: 100%;
        }

        header img {
            max-width: 100%;
            height: auto;
            width: 1986px;
            height: 182px;
        }

        .nav {
            display: flex;
            justify-content: center;
            background-color: rgba(0, 0, 0, 0.8);
            padding: 10px;
            width: 100%;
        }

        .nav a {
            color: white;
            text-decoration: none;
            padding: 10px 20px;
            margin: 0 10px;
        }

        .container {
            background-color: rgba(255, 255, 255, 0.8);
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
            max-width: 400px;
            width: 100%;
        }

        h2 {
            margin-top: 0;
        }

        form {
            text-align: left;
        }

        label {
            display: block;
            margin-bottom: 5px;
        }

        input {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            box-sizing: border-box;
        }

        button {
            background-color: #4caf50;
            color: white;
            padding: 10px 15px;
            border: none;
            cursor: pointer;
            width: 100%;
        }

        .options {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .options a {
            text-decoration: none;
            color: #4caf50;
        }

        .alert {
            color: red;
            font-size: 14px;
            text-align: center;
        }

        /* Responsive Styles */
        @media (max-width: 600px) {
            .container {
                max-width: 100%;
            }

            header img {
                width: 100%;
                height: auto;
            }
        }
    </style>
</head>

<body>
    <header>
        <a href="https://www.hostelworld.com/" target="_blank">
            <img src="/images/cryptostay_header.png" alt="Header Image" width="1986" height="182">
        </a>
    </header>

    ,<!--<div class="nav">
        <a href="/">Home</a>
        <a href="/about">About</a>
        <a href="/contact">Contact</a>
    </div>-->

    <div class="container">
        <ul>
            <% if (messages.sucess_msg) { %>
                <li class="alert">
                    <%= messages.sucess_msg %>
                </li>
                <% } %>
        </ul>
        <ul>
            <% if (messages.error) { %>
                <li class="alert">
                    <%= messages.error %>
                </li>
                <% } %>
        </ul>

        <h2>Login Page</h2>
        <form action="/users/login" method="POST">
            <label for="usn">USN:</label>
            <input type="text" id="usn" name="usn" placeholder="USN" required>

            <label for="password">Password:</label>
            <input type="password" id="password" name="password" placeholder="Password" required>

            <div class="options">
                <label>
                    <input type="checkbox" name="remember" id="remember">
                    Remember Me
                </label>
                <a href="/users/forgot-password">Forgot Password?</a>
            </div>

            <button type="submit">Login</button>
        </form>
        <p style="text-align:center; margin-top: 20px;">
            <a href="/users/register">Haven't Signed up? Signup</a>
        </p>
    </div>
</body>

</html>