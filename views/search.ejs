<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        header {
            text-align: center;
            margin: 0;
            padding: 10px 0;
            background-color: #2a2925;
        }

        header img {
            max-width: 100%;
            height: auto;
        }

        body {
            background-color: #D4C1A6;
            font-family: Arial, sans-serif;
        }

        .container {
            max-width: 1200px;
            margin: auto;
            padding: 20px;
            background-color: #f8f9fa;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.3);
            transition: box-shadow 0.3s, transform 0.3s;
            margin-bottom: 20px;
            background: linear-gradient(135deg, #2a2925 0%, #3e3c36 100%);
            color: #f8f9fa;
            overflow: hidden;
            position: relative;
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            bottom: 0;
            left: 0;
            background: rgba(0, 0, 0, 0.1);
            opacity: 0;
            transition: opacity 0.3s;
        }

        .card:hover::before {
            opacity: 1;
        }

        .card:hover {
            box-shadow: 0 0 30px rgba(0, 0, 0, 0.5);
            transform: translateY(-10px);
        }

        .card-img-top {
            border-top-left-radius: 15px;
            border-top-right-radius: 15px;
            height: 200px;
            object-fit: cover;
            transition: transform 0.3s;
        }

        .card:hover .card-img-top {
            transform: scale(1.05);
        }

        .card-body {
            padding: 20px;
            display: flex;
            flex-direction: column;
            background: rgba(0, 0, 0, 0.5);
        }

        .card-title {
            font-size: 1rem;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .card-text {
            color: #c7c7c7;
            margin-bottom: 10px;
        }

        .btn-primary {
            background-color: #4caf50;
            border-color: #4caf50;
        }

        .btn-primary:hover {
            background-color: #388e3c;
            border-color: #388e3c;
        }

        .btn-block {
            width: 100%;
        }

        .announcement {
            background-color: #007bff;
            color: #fff;
            text-align: center;
            margin-bottom: 5px;
            border-radius: 5px;
        }

        .book-available {
            background-color: #d4edda;
            border: 1px solid #c3e6cb;
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 10px;
        }

        .book-not-available {
            background-color: #da7c6e;
            border: 1px solid #c3e6cb;
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 10px;
        }

        /* Responsive Styles */
        @media (max-width: 600px) {
            .container {
                max-width: 100%;
                padding: 10px;
            }
        }
    </style>
</head>

<body>
    <header>
        <a href="https://www.hostelworld.com/" target="_blank">
            <img src="/images/cryptostay_header.png" alt="Header Image" width="1986" height="182">
        </a>
    </header>
    <div class="container">
        <form id="searchForm" action="/users/search" method="GET">
            <div class="input-group mb-3">
                <input type="text" id="searchInput" name="query" class="form-control" placeholder="Enter Hostel Name">
                <div class="input-group-append">
                    <button type="submit" class="btn btn-primary">Search</button>
                </div>
            </div>
        </form>
        <div class="row" id="cardRow">
            <% if (searchResults.length> 0) { %>
                <% searchResults.forEach(book=> { %>
                    <div class="col-md-4">
                        <div class="card">
                            <img src="<%= book.book_url %>" class="card-img-top" alt="Book Cover">
                            <div class="card-body">
                                <h5 class="card-title">Title: <%= book.title %>
                                </h5>
                                <p class="card-text"><strong>Owner:</strong>
                                    <%= book.author_name %>
                                </p>
                                <p class="card-text"><strong>On-Site:</strong>
                                    <%= book.published_year %>
                                </p>
                                <form action="/users/addtocart" method="GET">
                                    <input type="hidden" name="book_id" value="<%= book.book_id %>">
                                    <button type="submit" class="btn btn-primary btn-block">Add To Cart</button>
                                </form>
                            </div>
                        </div>
                    </div>
                    <% }); %>
                        <% } else { %>
                            <p>No results found.</p>
                            <% } %>
        </div>
    </div>
</body>

</html>